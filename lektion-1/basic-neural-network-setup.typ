#set text(16pt, font: "New Computer Modern")
#set par(justify: true, leading: 0.58em)
#set enum(numbering: "I.")
#set page(header: [
  #set text(size: 12pt)
  _Valdemar Støvring Storgaard_
  #h(1fr)
  Erhvervsakademi København
])

#set page(numbering: "- 1 -")
#show link: it => underline(text(it, blue))

= ML - Week 1

=== numpy for data arrays

=== tensorflow.keras.models.Sequential for model creation

=== tensorflow.keras.layers.Dense with activation functions

=== tensorflow.keras.optimizers.Adam with custom learning rate

=== Manual data encoding (one-hot for categorical variables)

=== Multi-class classification (3 classes: Dem=0, Rep=1, Ind=2)

=== Political prediction based on age, income, sex, religion

=== Sparse categorical crossentropy loss

=== Basic model training (400 epochs, batch_size=4)

=== Model evaluation and prediction
